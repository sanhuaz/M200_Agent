# M200 Agent v0.2.0 修改概述

## 版本定位

v0.2.0 是 PersonalAgent/M200 Agent 的本地个人智能体功能升级版。在 v0.1.0 已有的 Web/QQ
聊天、三层记忆、文档混合检索和漫画任务基础上，重点补充扩展管理、人格、管理员、文件产物、
用户隔离和任务清理能力。

项目仍定位为 Windows 本机、个人使用的原型，不是生产级多用户服务。

## 主要新增功能

### 1. 动态 Tools

- 新增动态 Tool Registry，统一装配内置 Tool、启用的 Python Tool 和文件创建 Tool。
- 支持本地 ZIP 及公开 GitHub 仓库或子目录导入。
- 导入后默认停用，不自动安装第三方依赖。
- 校验 ZIP 路径穿越、绝对路径、符号链接、清单格式和入口函数。
- 内置 Tool 不允许被导入包覆盖或删除。
- Python Tool 在后端进程内运行，因此只应启用经过人工审查的受信代码。

### 2. Agent Skills

- 新增 `skills/<name>/SKILL.md` 包结构和数据库登记。
- 启动时只向模型提供 Skill 名称与描述，完整说明通过 `load_skill` 按需加载。
- 每轮最多加载 3 个 Skill。
- 支持读取 Skill 根目录内的 `references/` 和 `assets/` 资源。
- `scripts/` 仅保存和展示，v0.2 不执行脚本。
- 支持模型自动选择和 QQ `/skill <name> <请求>` 手动调用。

### 3. 人格系统

- 新增人格创建、编辑、删除、编译和分配接口。
- 原始人格文本只用于编辑，不直接拼接到运行时系统提示词。
- 通过当前聊天模型编译为受限的 `PersonaStyle` 结构，并进行字段及注入风险校验。
- 人格只影响语气、称呼、详细程度、词汇、格式和普通行为风格，不能修改权限、工具、密钥、
  系统规则或文件根目录。
- 支持全局人格和指定用户人格；当前 Web 页面提供全局人格启用，用户级分配可通过 API 完成。

### 4. 长期记忆隔离与管理

- `memories` 新增 `global`/`user` 作用域和用户标识。
- 自动提取只写入当前用户记忆；全局记忆只能从本机管理接口创建。
- QQ 用户之间按 `user_id` 严格隔离，同时可召回全局记忆。
- 相同事实更新时归档旧值，相同内容只刷新最后出现时间。
- 支持按作用域、用户、状态和关键词筛选，并提供编辑、归档、恢复和删除接口。
- 归档、编辑和删除时同步处理对应的 Chroma 记忆向量。
- 疑似 API Key、Token、密码或密钥不会进入长期记忆。

### 5. 管理员管理

- 新增 `admin_identities` 数据表和 Web 管理接口。
- `local-owner` 永久存在且不可删除。
- QQ Owner 可以在本机管理页面新增或删除，修改后立即生效。
- `.env` 中的 `OWNER_QQ_IDS` 只用于初始化导入，数据库随后成为运行时权限来源。
- 管理写接口限制为回环地址访问；当前版本不提供完整 RBAC 或公网认证。

### 6. 文件创建与产物

- 新增 `create_files` Tool，可创建代码或文本文件，但不会执行生成内容。
- 文件保存到 `workspace/generated/<user-id>/<artifact-id>/`，不同用户相互隔离。
- 禁止绝对路径、`..`、UNC、子目录和重复文件名。
- 单文件上限 10 MiB，单次最多 100 个文件，总计不超过 50 MiB。
- 单文件直接生成产物，多文件自动打包 ZIP。
- 数据库记录所有者、会话、绝对路径、大小、类型和 SHA-256。
- Web 提供下载接口；QQ 私聊可发送生成产物。

### 7. 漫画下载链路修复

- 统一 Tool 结果信封，并兼容对象、数组、JSON 字符串和非法 JSON，修复数组结果触发
  `AttributeError: 'list' object has no attribute 'get'` 的问题。
- Owner 下载不再要求二次确认，直接创建持久任务；非 Owner 仍被拒绝。
- 保留单 Worker、失败重试、重启恢复、PDF 生成和 QQ 私聊发送。
- QQ 发送成功状态写入任务结果，避免仅凭中间错误提示判断最终结果。
- 仅在 Owner 明确指定任务后删除已发送的漫画产物，并保留任务审计记录。
- Web 任务页支持查看进度、取消、下载和显式删除本地文件。

### 8. 知识库与 Embedding 修复

- 在线 Embedding 按服务商每批最多 64 条分批请求，避免单次数组超限。
- 文档列表新增失败后的“重新索引”接口和前端操作。
- 继续支持 TXT、Markdown、HTML、文本型 PDF 和 DOCX。
- 继续使用 Chroma 语义召回、SQLite FTS5/jieba 关键词召回、RRF 融合及可选在线 Reranker。
- Embedding 配置切换使用影子集合重建，成功前继续使用旧索引，避免混合模型或维度。

### 9. 前端管理页面

- 页面入口包括 `/chat`、`/knowledge`、`/tools`、`/skills`、`/personas`、`/memories`、
  `/admin`、`/tasks` 和 `/status`。
- 增加 Tool/Skill 导入和启停、人格编辑、记忆筛选与删除、QQ Owner 管理、任务产物管理等界面。
- 前端开发端口由 5173 调整为 5176，后端 CORS 和启动脚本同步更新。

### 10. 数据迁移与启动

- 新增 Alembic `0002_v020` 迁移，创建扩展、人格、管理员、产物和设置相关数据表。
- 启动脚本固定使用 `D:\miniconda\envs\langchain1.2\python.exe` 并校验 Python 3.13。
- 启动前自动判断数据库状态、备份旧运行数据、执行迁移并校验 Schema。
- 运行数据备份存放在 `_v020_backups/`，不会进入 Git。

## 验证结果

发布前执行：

```powershell
.\scripts\check.ps1
```

结果：

- `pip check`：通过。
- Python `compileall`：通过。
- Ruff：通过。
- Pyright：0 errors、0 warnings。
- Pytest：26 项通过；存在 1 条来自 FastAPI TestClient/httpx 的上游弃用警告。
- Vue TypeScript 检查：通过。
- Vite 构建：通过；存在单个前端产物超过 500 kB 的体积警告。

自动测试不会访问真实模型、真实 JMComic 或真实 QQ。真实第三方链路的可用性仍取决于本机 `.env`、
NapCat 配置、账号权限、网络以及上游服务状态。

## 已知边界

- 不包含 Docker、Redis、Celery、多进程 Worker、完整 RBAC、生产日志平台、指标或链路追踪。
- 不包含 OCR、PPTX/XLSX 解析、图片或语音模型。
- Web 管理端依赖应用只监听本机回环地址，不应直接暴露到局域网或公网。
- 导入并启用的 Python Tool 拥有后端进程权限，不是安全沙箱。
- 前端目前仍为单个 `App.vue` 管理界面，尚未拆分为独立路由组件。
- 用户专属人格分配已有后端接口，但当前 Web 页面只提供全局人格启用入口。
