[tool.pytest.ini_options]
addopts = "-q -p no:cacheprovider"
pythonpath = ["backend"]
testpaths = ["backend/tests"]

[tool.ruff]
line-length = 110
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "PIE", "ISC"]
ignore = ["B008", "BLE001"]
