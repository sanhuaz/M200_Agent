# PersonalAgent / M200 Agent v0.2.0 项目交接文档

> 本文用于把当前项目交接给新的 Codex 窗口或后续维护者。先阅读“当前状态”和“不可误判的边界”，
> 再开始修改。不要把计划功能、自动测试或页面能打开误写成真实外部链路成功。

## 1. 当前状态

- 项目目录：`E:\Codex\PersonalAgent`
- GitHub：<https://github.com/sanhuaz/M200_Agent>
- 分支：`main`
- 当前发布版本：`v0.2.0`
- v0.2 核心发布提交：`ce549e3`（`feat: release M200 Agent v0.2.0`）
- 核心发布提交已于 2026-08-18 推送到 `origin/main`。
- 本交接文档作为发布后的文档提交推送，`v0.2.0` 标签指向包含本文档的最终交接状态。
- 数据库迁移版本：`0002_v020 (head)`。
- 固定 Python：`D:\miniconda\envs\langchain1.2\python.exe`
- Python 版本：3.13.12。
- 前端开发端口：5176。
- 后端端口：8000。
- NapCat WebUI 常用端口：6099，由 NapCat 自己管理，不属于本项目启动脚本。
- 交接时 8000、5176、6099 均未监听，项目和 NapCat 当前处于关闭状态。

不要执行 `git reset --hard`、强制覆盖、递归删除或重新初始化仓库。运行数据和 `.env` 不在 Git 中，
但对本机功能很重要。

## 2. 项目定位

PersonalAgent 对外仓库名为 M200 Agent，是一个 Windows 本机、个人使用的 AI Agent 原型。当前版本
提供 Web 与 QQ 对话、长期记忆、知识库 RAG、动态 Tools/Skills、人格、管理员、文件创建和漫画
下载任务。

它不是生产级多用户服务，默认只监听 `127.0.0.1`。当前不包含 Docker、Redis、Celery、完整
RBAC、生产部署、日志平台、指标、链路追踪、OCR、PPTX/XLSX、图片或语音模型。

## 3. 技术栈

### 后端

- Python 3.13.12。
- FastAPI 0.135.1、Uvicorn 0.46.0。
- SQLAlchemy 2.0.51、Alembic 1.19.1。
- LangChain 1.2.12、LangGraph 1.1.2、`langgraph-checkpoint-sqlite` 3.1.1。
- SQLite 业务库、FTS5 关键词索引、SQLite LangGraph Checkpoint。
- ChromaDB 1.5.9、Sentence Transformers 5.7.0、jieba 0.42.1。
- pypdf、python-docx、BeautifulSoup 用于文档解析。
- JMComic 2.7.4 用于漫画搜索和下载。

### 前端

- Vue 3.5、TypeScript 5.9、Vite 7、Element Plus 2。
- `frontend/package.json` 版本已更新为 `0.2.0`。
- 当前界面仍集中在 `frontend/src/App.vue`，通过 History 路径和标签切换页面，不是已经拆分好的
  Vue Router 多组件架构。

### 外部接入

- OpenAI 兼容聊天模型，要求支持原生 Tool Calls。
- 本地 BGE 或 OpenAI 兼容在线 Embedding。
- 可选 SiliconFlow 兼容 Reranker 接口。
- NapCat OneBot v11 WebSocket Client。

## 4. 关键目录

```text
PersonalAgent/
├─ backend/
│  ├─ alembic/versions/
│  │  ├─ 0001_initial.py       # 已固化的 v0.1 Schema
│  │  └─ 0002_v020.py          # v0.2 扩展迁移
│  ├─ app/
│  │  ├─ api/routes.py         # Web API
│  │  ├─ api/onebot.py         # OneBot WS、QQ 命令和文件发送
│  │  ├─ core/config.py        # 配置与路径解析
│  │  ├─ db/models.py          # ORM 实体
│  │  ├─ services/             # 记忆、检索、任务、扩展、人格、产物等
│  │  └─ workflows/agent.py    # LangGraph 与动态工具注册
│  └─ tests/                   # 26 项后端测试
├─ frontend/
│  └─ src/App.vue              # 当前管理端主体
├─ scripts/
│  ├─ start.ps1                # 迁移后启动后端和前端
│  ├─ migrate.ps1              # 备份、stamp/upgrade
│  └─ check.ps1                # 完整离线检查
├─ data/                       # 数据库、Chroma、上传和下载，Git 忽略
├─ tools/                      # 已安装 Tool，内容默认 Git 忽略
├─ skills/                     # 已安装 Skill，内容默认 Git 忽略
├─ workspace/generated/        # Agent 生成文件，Git 忽略
├─ _v020_backups/              # 迁移备份，Git 忽略
├─ .env                        # 真实配置和密钥，Git 忽略
├─ .env.example                # 脱敏模板
├─ README.md
└─ v0.2.0-修改概述.md
```

## 5. 已完成功能

### 5.1 Agent 对话与模型

- Web 使用 `POST /api/v1/chat/stream` 返回 SSE。
- SSE 实际事件为：`token`、`tool_started`、`tool_finished`、`pending_confirmation`、
  `task_created`、`artifact_created`、`final`、`error`。
- 当前没有单独的 `skill_loaded` 事件；Skill 加载会表现为普通 Tool 事件。
- 模型配置保存在 `MODEL_PROFILES_JSON`，API Key 通过 `api_key_env` 指向环境变量，不存数据库。
- 模型配置支持别名、模型名、`base_url`、上下文长度和超时。
- 每个会话单独保存模型别名，切换只影响目标会话。
- 使用 LangGraph `StateGraph` 和原生 Function Calling：模型节点与 ToolNode 循环，直到生成最终回答。
- Tool 结果统一包装为 `kind/data/pending_confirmation/artifact_ids` 信封。
- ToolMessage 解析兼容对象、数组、JSON 字符串、普通字符串和非法 JSON。
- LangGraph Checkpoint 保存到 `data/langgraph_checkpoints.db`。

### 5.2 对话上下文与摘要

- 运行时系统上下文包含固定系统规则、当前用户记忆、知识库清单、可用 Skill 描述、历史摘要和人格。
- 每次对话读取最近 17 条业务消息。
- 消息超过 18 条后，后台使用当前会话模型摘要较早内容，并保留最近 8 条作为未摘要部分。
- 业务消息继续保存在 `messages` 表，Checkpoint 不替代业务消息表。

### 5.3 长期记忆

- 自动记忆提取在最终回答保存后后台执行，失败不会影响当前回复。
- 使用当前会话模型的结构化 Function Calling 提取稳定事实、长期偏好和明确长期事件。
- DeepSeek 官方 API 会在记忆提取时关闭 thinking，避免结构化输出兼容问题。
- 疑似 API Key、Token、密码、Secret 或密钥会被拒绝写入。
- 同一用户、同一事实键和相同内容只更新 `last_seen_at`。
- 同一事实键出现新内容时归档旧值并启用新值。
- 自动提取只写 `scope_type=user`；全局记忆只能通过本机管理接口手动创建。
- 召回范围是“全局记忆 + 当前用户记忆”，QQ 用户之间不会共享用户记忆。
- 正常使用 Chroma 向量召回；Embedding 不可用时降级为关键词匹配，不阻塞聊天。
- Web 支持作用域、用户、状态和关键词筛选，以及创建、编辑、归档和删除。
- 恢复归档记忆已有 API，当前页面没有单独的恢复按钮。

### 5.4 文档知识库与混合检索

- 支持 TXT、Markdown、HTML、文本型 PDF、DOCX。
- 扫描 PDF 不做 OCR，PPTX/XLSX 尚未实现。
- 保存文件名、SHA-256、标题路径、页码、位置和文档状态。
- 使用结构优先切块，并保留边界重叠。
- 查询执行 Chroma 语义召回 20 条和 SQLite FTS5/jieba 关键词召回 20 条。
- 使用 RRF 合并去重，最终返回 5 条证据。
- 可选在线 Reranker；关闭、成功和失败降级状态可以区分。
- 回答提示要求引用文件、标题、页码或段落位置。
- 在线 Embedding 每批最多发送 64 条，修复服务商数组上限错误。
- 知识库切换 Embedding 时使用影子集合重建，新索引成功前保留旧索引。
- 文档失败后可以通过 API 和 Web“重新索引”。

### 5.5 动态 Tools

- 内置 Tool：知识库搜索、漫画搜索/下载/删除、文件创建、Skill 加载与资源读取。
- Python Tool 包结构为 `tools/<name>/tool.json`、`plugin.py`、可选 README。
- 入口函数为 `create_tools(context) -> list[BaseTool]`。
- 支持本地 ZIP 和公开 GitHub 仓库或子目录导入。
- 禁止任意非 GitHub URL、ZIP Slip、绝对路径、符号链接和覆盖内置 Tool。
- 导入后默认 `installed_disabled`，不自动安装依赖。
- 启用、停用、删除操作经过 Owner 确认流程。
- Python Tool 在后端进程内执行，拥有本机代码权限，不是沙箱。

### 5.6 Agent Skills

- Skill 使用 `SKILL.md`，可带 `references/`、`assets/`、`scripts/`。
- 启动/建图时只向模型提供启用 Skill 的名称和描述。
- 模型调用 `load_skill(name)` 后才读取完整 `SKILL.md`，每轮最多加载 3 个。
- `read_skill_resource` 只能读取该 Skill 根目录内资源。
- `scripts/` 只保存和展示，v0.2 不执行。
- Skill 不能提升权限、修改系统规则、人格、密钥或文件根目录。
- 支持自动工具调用和 QQ `/skill <name> <请求>` 手动调用。
- 支持 ZIP/GitHub 导入、启用、停用和删除。

### 5.7 人格

- 支持创建、编辑、删除、编译和分配人格。
- 原始人格文本只用于编辑，不直接拼接到运行时系统提示词。
- 编译使用当前聊天模型，输出受限的结构化 PersonaStyle。
- 人格只能控制语气、称呼、详细程度、词汇、格式和普通行为风格。
- 人格不能控制 Tool、Owner 权限、文件目录、API Key 或系统规则。
- 优先级为指定用户人格、全局人格、默认无人格。
- 后端已支持全局和指定用户分配。
- 当前 Web 页面只提供“启用全局”，尚未提供指定用户人格分配控件。

### 5.8 管理员

- `local-owner` 永久存在且不可删除。
- QQ Owner 保存在 `admin_identities`，增删后立即生效，不需要重启。
- `.env` 的 `OWNER_QQ_IDS` 只在初始化时导入数据库，不应长期作为唯一权限来源。
- Web 管理页面可以新增和删除 QQ Owner。
- 本机管理写接口只允许回环地址。
- 当前没有完整登录系统或 RBAC，因此不得直接暴露公网。

### 5.9 文件创建与产物

- `create_files` 仅允许私聊使用，群聊禁止。
- 保存根目录为 `workspace/generated/<user-id>/<artifact-id>/`。
- 禁止绝对路径、`..`、UNC、子目录、非法或重复文件名。
- 单文件最多 10 MiB，单次最多 100 个文件，总计最多 50 MiB。
- 单文件直接保存，多文件生成 ZIP。
- 生成代码只保存，不自动执行。
- `artifacts` 表保存所有者、会话、文件名、路径、大小、内容类型和 SHA-256。
- Web 支持产物列表和下载；QQ 私聊可以发送产物。

### 5.10 漫画任务

- 漫画搜索不会自动下载。
- 只有 Owner 可以创建下载任务。
- Owner 下载不再需要二次确认，直接进入 SQLite 持久队列。
- Worker 单并发，任务状态为 `queued/running/succeeded/failed/cancelled`。
- 服务启动时将遗留 `running` 恢复为 `queued`。
- 瞬时失败最多重试两次，默认生成 PDF。
- 文件保存到 `data/downloads/<job_id>`。
- QQ Owner 私聊任务成功后自动发送文件。
- 超过 `QQ_UPLOAD_LIMIT_MB` 或发送失败时保留本地路径。
- 仅在 Owner 明确指定任务后删除已成功发送的漫画文件，任务审计记录保留。
- 修复了漫画搜索返回数组导致 `.get()` 崩溃的问题。

### 5.11 NapCat / OneBot v11

- WebSocket：`ws://127.0.0.1:8000/api/v1/onebot/ws`。
- Token 必须与 `.env` 中 `ONEBOT_TOKEN` 完全一致，否则握手返回 403。
- 私聊默认回复。
- 群聊仅在 @机器人或 `/ai` 前缀时回复。
- 会话按私聊用户或群号隔离，只保存定向发给机器人的群消息。
- 使用 OneBot `message_id` 去重，避免重连重复处理。
- 支持文本回复、Owner 私聊文件发送和任务完成后的自动发送。
- 命令包括 `/help`、`/model list`、`/model use`、`/kb`、`/memory`、
  `/memory delete`、`/tools`、`/skills`、`/skill`、`/jm`、`/jm download`、
  `/jm delete`、`/confirm`、`/cancel`。
- NapCat 网络配置由 NapCat 自身持久化；本项目脚本不会创建、删除或修改 NapCat Bot 配置。
- 停止项目时不要删除 NapCat Bot、账号目录或网络配置。

### 5.12 Web 管理端

- `/chat`：SSE 对话、会话列表、模型选择。
- `/knowledge`：知识库、上传、索引状态、Embedding 切换、重新索引和删除。
- `/tools`：Tool 导入、启停和删除。
- `/skills`：Skill 导入、启停和删除。
- `/personas`：人格创建、编辑、编译和全局启用。
- `/memories`：长期记忆筛选、编辑、归档和删除。
- `/admin`：QQ Owner 增删。
- `/tasks`：漫画搜索、下载任务、取消、产物下载和删除。
- `/status`：数据库、Python、Chroma、Worker、OneBot、模型、Embedding 和 Reranker 状态。

## 6. 数据库实体与迁移

主要表：

```text
conversations
messages
knowledge_bases
documents
chunks
memories
confirmations
jobs
tool_runs
processed_events
extension_packages
personas
persona_assignments
admin_identities
artifacts
app_settings
```

另外使用 FTS5 虚拟表 `chunk_fts`。

迁移说明：

- `0001_initial.py` 已从动态读取当前 ORM 改为显式、冻结的 v0.1 Schema。
- `0002_v020.py` 增加记忆作用域和 v0.2 数据表。
- 当前本机数据库已经在 `0002_v020 (head)`。
- `scripts/migrate.ps1` 会识别空库、无 Alembic 版本的旧库和已有版本库。
- 旧库会先备份，再 `stamp 0001_initial`，最后升级到 head。
- 迁移前备份数据库、Checkpoint、Chroma、上传和下载目录到 `_v020_backups/`。
- 迁移失败时 `start.ps1` 不会继续启动后端。

不要手动删除 `alembic_version`，也不要再次让 `0001_initial.py` 动态导入当前 ORM。

## 7. API 清单

所有 HTTP API 前缀为 `/api/v1`：

```text
GET    /health
GET    /models
POST   /conversations
GET    /conversations
GET    /conversations/{conversation_id}/messages
PUT    /conversations/{conversation_id}/model
POST   /chat/stream

POST   /knowledge-bases
GET    /knowledge-bases
PUT    /knowledge-bases/{knowledge_base_id}/embedding
DELETE /knowledge-bases/{knowledge_base_id}
POST   /documents/{knowledge_base_id}
GET    /documents
POST   /documents/{document_id}/reindex
DELETE /documents/{document_id}

GET    /memories
POST   /memories
PUT    /memories/{memory_id}
POST   /memories/{memory_id}/archive
POST   /memories/{memory_id}/restore
DELETE /memories/{memory_id}

POST   /manga/search
POST   /manga/download
POST   /confirmations/{token}
GET    /confirmations
GET    /tasks
POST   /tasks/{job_id}/cancel
GET    /tasks/{job_id}/artifact
DELETE /tasks/{job_id}/artifact

GET    /tools
GET    /tools/{name}
POST   /tools/import
POST   /tools/import/github
POST   /tools/{name}/enable
POST   /tools/{name}/disable
DELETE /tools/{name}

GET    /skills
GET    /skills/{name}
POST   /skills/import
POST   /skills/import/github
POST   /skills/{name}/enable
POST   /skills/{name}/disable
DELETE /skills/{name}

GET    /personas
POST   /personas
PUT    /personas/{persona_id}
POST   /personas/{persona_id}/compile
DELETE /personas/{persona_id}
GET    /personas/assignments
PUT    /personas/assignments/global
PUT    /personas/assignments/users/{user_id}

GET    /admins
GET    /identities
POST   /admins
DELETE /admins/{external_id}

GET    /artifacts
GET    /artifacts/{artifact_id}/download
WS     /onebot/ws
```

带本机管理限制的写接口通过 `require_loopback` 检查客户端地址。该检查不是完整身份认证。

## 8. 配置项

从 `.env.example` 复制到 `.env`，不要把真实值写进 Git、README、聊天记录或日志。

```text
APP_ENV
FRONTEND_ORIGIN
DATABASE_PATH
CHROMA_PATH
UPLOAD_PATH
DOWNLOAD_PATH
TOOLS_PATH
SKILLS_PATH
WORKSPACE_PATH

MODEL_PROFILES_JSON
PERSONAL_AGENT_LLM_API_KEY 或模型配置引用的其他环境变量

DEFAULT_EMBEDDING_PROFILE
LOCAL_EMBEDDING_MODEL
ONLINE_EMBEDDING_MODEL
ONLINE_EMBEDDING_BASE_URL
ONLINE_EMBEDDING_API_KEY_ENV
PERSONAL_AGENT_EMBEDDING_API_KEY 或所引用的环境变量

RERANK_ENABLED
RERANK_URL
RERANK_MODEL
RERANK_API_KEY

ONEBOT_TOKEN
OWNER_QQ_IDS
QQ_UPLOAD_LIMIT_MB
GROUP_COMMAND_PREFIX
```

注意：

- `MODEL_PROFILES_JSON` 中保存的是密钥环境变量名，不是密钥值。
- `ONEBOT_TOKEN` 是自定义的长期随机字符串，不是 NapCat WebUI Token。
- NapCat WebSocket Client Token 必须和 `ONEBOT_TOKEN` 相同。
- Owner QQ 是向机器人发消息的个人 QQ，不是机器人 QQ。
- 不要在交接文档中复制当前 `.env` 的真实内容。

## 9. 完整启动流程

### 9.1 首次准备

```powershell
Set-Location "E:\Codex\PersonalAgent"
Copy-Item ".env.example" ".env"
& "D:\miniconda\envs\langchain1.2\python.exe" -m pip install --no-user -r requirements.txt
Set-Location "E:\Codex\PersonalAgent\frontend"
pnpm install
Set-Location "E:\Codex\PersonalAgent"
```

然后仅在本机编辑 `.env`，填写模型、Embedding、Reranker、OneBot 和 Owner 配置。

### 9.2 日常启动

```powershell
Set-Location "E:\Codex\PersonalAgent"
.\scripts\start.ps1
```

该脚本会：

1. 固定并校验 `D:\miniconda\envs\langchain1.2\python.exe` 和 Python 3.13。
2. 执行迁移与必要备份。
3. 启动后端 `127.0.0.1:8000`。
4. 启动前端 `127.0.0.1:5176`。

访问：

- Web：<http://127.0.0.1:5176>
- OpenAPI：<http://127.0.0.1:8000/docs>
- Health：<http://127.0.0.1:8000/api/v1/health>

### 9.3 NapCat

NapCat 需要单独启动并登录机器人账号。在 NapCat WebUI 中确保已有持久的 WebSocket Client：

```text
URL: ws://127.0.0.1:8000/api/v1/onebot/ws
Token: 与项目 .env 中 ONEBOT_TOKEN 完全一致
心跳间隔: 30000
重连间隔: 5000
消息格式: Array
```

若 Health 显示：

- `connected`：已连接。
- `configured_disconnected`：项目 Token 已配置，但 NapCat 未连接。
- `needs_configuration`：项目仍使用默认 Token 或未配置。

403 基本表示两端 Token 不一致。不要使用 NapCat WebUI 登录 Token 代替 `ONEBOT_TOKEN`。

## 10. 停止流程

项目没有单独的 `stop.ps1`。停止时按端口精确查找进程，不要删除配置目录：

```powershell
$ports = 8000, 5176
foreach ($port in $ports) {
    Get-NetTCPConnection -LocalPort $port -State Listen -ErrorAction SilentlyContinue |
        Select-Object -ExpandProperty OwningProcess -Unique |
        ForEach-Object { Stop-Process -Id $_ }
}
```

NapCat 只在用户明确要求时单独退出。不要删除 Bot、账号或网络配置。停止后检查：

```powershell
Get-NetTCPConnection -LocalPort 8000,5176,6099 -State Listen -ErrorAction SilentlyContinue
```

## 11. 验证结果

2026-08-18 发布前执行：

```powershell
Set-Location "E:\Codex\PersonalAgent"
.\scripts\check.ps1
```

结果：

- `pip check`：通过，`No broken requirements found.`
- Python `compileall`：通过。
- Ruff：通过。
- Pyright：0 errors、0 warnings。
- Pytest：26 项通过。
- Vue TypeScript：通过。
- Vite build：通过，1591 个模块完成转换。
- Alembic 当前版本：`0002_v020 (head)`。

非失败警告：

- FastAPI TestClient/httpx 有 1 条上游弃用警告。
- Vite 产物中主 JS 约 1008 kB，超过 500 kB，提示后续可做代码分割。

开发期间用户已对大部分真实链路独立测试并反馈无问题，包括普通对话、NapCat/QQ、漫画下载和
文件接收等。但本次发布动作没有重新启动外部服务执行全链路，因此应明确区分：

- 本轮确认：完整离线检查、迁移版本、脱敏检查、Git 提交和 GitHub 推送。
- 历史/用户联调：真实模型、Embedding、Reranker、JMComic、NapCat 和 QQ。
- 任何未来真实测试都可能受密钥、余额、账号、网络、NapCat 登录和上游接口变化影响。

## 12. 自动测试覆盖重点

- 固定 Python 解释器健康检查。
- 会话创建、历史和会话级模型切换。
- SSE、LangGraph SQLite Checkpoint。
- Tool 数组/非法结果解析。
- 漫画 Owner 直下、不再确认、任务 API。
- PDF 多页生成、无图片失败、发送后删除和删除权限。
- QQ 文件发送结果记录。
- OneBot 权限、确认时间和 Owner 判断。
- 文档切块、FTS 行隔离和重新索引。
- 在线 Embedding 64 条批处理。
- RRF 融合。
- Artifact 路径和大小限制。
- Skill ZIP 校验、默认停用和 GitHub 来源限制。
- 用户记忆隔离。

## 13. Git 与发布状态

- v0.1.0 提交：`238f798`，已有 `v0.1.0` 标签。
- v0.2.0 核心提交：`ce549e3`，已推送 `origin/main`。
- `v0.2.0` 标签指向包含交接文档的最终发布状态，并已推送到远端。
- `.env`、数据库、Chroma、上传文档、下载文件、生成产物、迁移备份和 NapCat 配置均未提交。

发布或继续开发前先执行：

```powershell
git status --short --branch
git log -5 --oneline --decorate
git remote -v
```

不要把工作树里用户自己的改动覆盖掉，也不要未经请求自动 force push。

## 14. 不可误判的边界

- 页面能打开不等于模型、Tool、RAG、任务或 QQ 真正成功。
- HTTP 200 不等于返回内容正确。
- NapCat 日志显示“收到消息”不等于 Agent 已最终回复。
- 任务进入 `queued` 不等于下载、PDF 或发送已经完成。
- Tool 有调用日志不等于工具结果已被模型正确回填。
- 自动测试不会调用真实模型、真实 Embedding、真实 JMComic 或真实 QQ。
- Reranker 失败时必须显示降级到 RRF，不能伪装为重排成功。
- Embedding 失败时不能写零向量或混合不同模型维度。
- 人格不是系统提示词，不能修改权限和工具边界。
- Skill 包内脚本在 v0.2 不能执行。
- 导入 Python Tool 等同于允许受信代码进入后端进程。
- 当前 Web 的回环限制不是完整权限系统，不能直接暴露公网。

## 15. 已知待完善项

这些不是 v0.2 发布阻塞项，但后续窗口不得误写为已经完成：

1. 前端仍是单个 `App.vue`，可在后续按页面拆成 Vue Router 组件并做懒加载。
2. Web 尚无指定用户人格分配控件，只有后端 API。
3. Web 对归档记忆没有恢复按钮，只有恢复 API。
4. 前端主 JS 包较大，可按页面拆包。
5. FastAPI TestClient/httpx 有上游弃用警告，等待依赖链适配或按官方迁移。
6. 没有完整登录、RBAC、CSRF、防滥用、限流和生产审计。
7. 没有 Tool 子进程沙箱；启用导入 Tool 前必须人工审查。
8. 没有 OCR、PPTX/XLSX、多进程队列、生产部署和可观测性平台。
9. 真实第三方链路应在 `.env`、NapCat 和账号准备好后按业务结果重新验收。

## 16. 建议的新窗口首条指令

可以把下面内容作为新窗口的第一条消息：

```text
请先阅读 E:\Codex\PersonalAgent\项目交接文档.md、README.md 和
v0.2.0-修改概述.md，再检查 git status、当前端口和 .env 配置是否存在。
项目是 M200 Agent v0.2.0，本机固定使用
D:\miniconda\envs\langchain1.2\python.exe。不要读取或输出 .env 的密钥，
不要覆盖运行数据，不要删除 NapCat Bot 或网络配置。开始工作前先说明你核对到的当前状态，
并区分离线检查与真实外部链路验证。
```
